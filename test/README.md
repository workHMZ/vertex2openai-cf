# Tests

Unit tests for the request/response converters and the Worker router.

They run on Node's built-in test runner. Source files are bundled with the
esbuild binary that ships inside `wrangler`, so the suite adds no new
dependencies.

```bash
npm test
```
